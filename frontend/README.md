# SRS Analyzer Frontend

React + Vite frontend for the Requirement Workspace.

## Run
```bash
npm install
npm run dev
```

Backend is expected at `http://localhost:8080/api`.

## Roles
- `user / user123` → requirement workspace + AI preference + analysis.
- `admin / admin123` → workspace + Administration dashboard with AI prompt/token audit.
